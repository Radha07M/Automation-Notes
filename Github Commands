https://github.com/joshnh/Git-Commands

#Git Configuration=>
git config --global user.name "Radha07M"
git config --global user.email "radha07mane@gmail.com"

#Git initialization (Local repository creation so this will create .git folder in your project)=>
git init

#Git status (used to check the currect status of files in currect branch)=>
git status

#Clone a repo (Clone remote repository in your system)=>
git clone https://github.com/Radha07M/JavaPrograms.git

#Git checkout (Switch to or Move to branch)=>
git checkout <branchName>      ---> Switch to branch
git checkout -b <branchName>   ---> Create a new branch and switch to newly created branch
git checkout -- <fileName>     ---> Discard the file changes

#Git add (Add changes to staging area. Used to add index to the changes.)=>
git add --all    OR      git add -A
git add <fileName>     ---> this will add specified file to staging area

#Git commit (used to add staged changes to local repository)=>
git commit -m <Commit message>

#Git push (used to push the local repository changes to remote repository)=>
git push origin <branchName>    ---> Push a branch to your remote repository
git push origin                 ---> push changes to remote repository

#Create new branch=> 
git branch <branchName>       ---> Create a new branch with given name
git branch                    ---> List the all branches. * indicates currect branch
git branch -d <branchName>    ---> Delete a Branch

#Git fetch (Fetch the changes into local repository)=>
git merge [branch name]	Merge a branch into the active branch
git merge [source branch] [target branch]	Merge a branch into a target branch
git stash	Stash changes in a dirty working directory
git stash clear	Remove all stashed entries

git pull	Update local repository to the newest commit
git pull origin [branch name]	Pull changes from remote repository
git remote add origin ssh://git@github.com/[username]/[repository-name].git	Add a remote repository
git remote set-url origin ssh://git@github.com/[username]/[repository-name].git	Set a repository's origin branch to SSH
Inspection & Comparison
Command	Description
git log	View changes
git log --summary	View changes (detailed)
git log --oneline	View changes (briefly)
git diff [source branch] [target branch]	Preview changes before merging
